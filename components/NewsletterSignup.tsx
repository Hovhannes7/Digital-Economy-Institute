import { Button } from "@/components/Button";
import { NoticeBox } from "@/components/NoticeBox";
const topics = ["Publications","Targeted AI","Digital Economy","Agriculture Community","Healthcare Community","Environment Community","Jurisprudence Community","AI & Blockchain Community","Events","Partner updates"];
export function NewsletterSignup({ full = false }: { full?: boolean }) {
  return <form className="rounded-3xl border border-line bg-white p-6 shadow-subtle"><div className={full ? "grid gap-5 md:grid-cols-2" : "space-y-4"}><label className="block text-sm font-semibold">Email address<input type="email" placeholder="you@example.com" className="mt-2 w-full rounded-xl border border-line px-4 py-3 text-sm outline-none focus:border-ink"/></label>{full ? <label className="block text-sm font-semibold">Name<input type="text" placeholder="Full name" className="mt-2 w-full rounded-xl border border-line px-4 py-3 text-sm outline-none focus:border-ink"/></label> : null}</div>{full ? <fieldset className="mt-6"><legend className="text-sm font-semibold">Topics</legend><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{topics.map(t => <label key={t} className="flex items-center gap-2 rounded-xl border border-line p-3 text-sm"><input type="checkbox"/> {t}</label>)}</div></fieldset> : null}<div className="mt-6 flex flex-wrap items-center gap-3"><Button type="button">Subscribe</Button><span className="text-xs text-steel">Frontend prototype only</span></div>{full ? <div className="mt-5"><NoticeBox>Newsletter integration can later be added with Mailchimp, Buttondown, Resend, or ConvertKit. This MVP does not send or store subscription data.</NoticeBox></div> : null}</form>;
}
