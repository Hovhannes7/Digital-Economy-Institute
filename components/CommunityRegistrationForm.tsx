import { Button } from "@/components/Button";
import { NoticeBox } from "@/components/NoticeBox";
export function CommunityRegistrationForm({ communityName }: { communityName: string }) {
  return <form id="registration" className="rounded-3xl border border-line bg-white p-6 shadow-subtle"><h2 className="text-2xl font-bold">Register for {communityName}</h2><p className="mt-2 text-sm leading-6 text-steel">Use email or phone number to express interest in this community.</p><div className="mt-6 grid gap-4 md:grid-cols-2"><label className="block text-sm font-semibold">Email address<input type="email" placeholder="you@example.com" className="mt-2 w-full rounded-xl border border-line px-4 py-3 text-sm outline-none focus:border-ink"/></label><label className="block text-sm font-semibold">Phone number<input type="tel" placeholder="+374 ..." className="mt-2 w-full rounded-xl border border-line px-4 py-3 text-sm outline-none focus:border-ink"/></label></div><div className="mt-6"><Button type="button">Register to Participate</Button></div><div className="mt-5"><NoticeBox>Registration is shown as a frontend prototype in this version. Production use requires authentication and secure backend integration.</NoticeBox></div></form>;
}
