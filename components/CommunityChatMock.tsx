import { Button } from "@/components/Button";
import { NoticeBox } from "@/components/NoticeBox";
export function CommunityChatMock({ communityName }: { communityName: string }) {
  const messages = ["Can we compare this dataset with a previous regional study before drawing conclusions?","Please add assumptions and source notes so the analysis can be reproduced.","I recommend expert review before this becomes a publication note."];
  return <section className="rounded-3xl border border-line bg-white p-6 shadow-subtle"><h2 className="text-2xl font-bold">{communityName} chat</h2><p className="mt-2 text-sm leading-6 text-steel">Frontend interface for text messages, file attachments, photo sharing, and voice notes.</p><div className="mt-6 space-y-3">{messages.map((m,i) => <div key={m} className="rounded-2xl bg-paper p-4"><p className="text-xs font-bold uppercase tracking-wide text-steel">Demo member {i+1}</p><p className="mt-2 text-sm">{m}</p></div>)}</div><label className="mt-6 block text-sm font-semibold">Write a message<textarea rows={3} placeholder="Type a research question or comment..." className="mt-2 w-full rounded-xl border border-line px-4 py-3 text-sm outline-none focus:border-ink"/></label><div className="mt-4 flex flex-wrap gap-2">{["Attach file","Attach photo","Upload voice note","Record voice"].map(a => <button key={a} type="button" className="rounded-full border border-line px-4 py-2 text-xs font-semibold">{a}</button>)}</div><div className="mt-5 flex flex-wrap items-center gap-3"><Button type="button">Send demo message</Button><span className="text-xs text-steel">UI only — no data is stored.</span></div><div className="mt-5"><NoticeBox tone="warning">Community messages, files, photos, and voice notes are designed to be automatically and permanently deleted after 48 hours in the production version. This MVP shows the interface only; real deletion requires backend storage and scheduled deletion logic.</NoticeBox></div></section>;
}
